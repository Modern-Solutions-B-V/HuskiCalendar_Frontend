## Available Scripts

to run this project follow these steps:
    -you should have xampp or wampp.
    -you should start apache and mysql.
    -open two terminals : first terminal -> cd backend ->`npm install --f` ->`node index`
                          second terminal -> cd frontend -> `npm install --f` -> `npm start`
